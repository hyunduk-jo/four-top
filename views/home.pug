//- extends layouts/main
//- include mixins/videoBlock

//- block content
//-     .videos
//-         each item in videosDB
//-             +videoBlock({
//-                 id:item.id,
//-                 title:item.title,
//-                 views:item.views,
//-                 videoFile:item.fileUrl
//-             })

extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each item in videosDB
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })
            